name: React (vite) CI

on: 
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Set Up Node js
        uses: actions/setup-node@v4
        with: 
          node-version: '20.x'
      
      - name: Install Dependencies
        uses: npm install

      - name: Run React App
        uses: npm run dev